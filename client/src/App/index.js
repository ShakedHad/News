import React from 'react';

const styles = {
    app: {
        paddingTop: 40,
        textAlign: 'center'
    }
};

export default () => {
    return <div style={styles.app}>Welcome to React!</div>;
};
